package me.comu.exeter.database;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

class SQLDataSource {
    private static final Logger LOGGER = LoggerFactory.getLogger(SQLDataSource.class);
}
